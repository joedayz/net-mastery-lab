# Clean Code y Buenas Prácticas 💎

## Introducción

Clean Code es un conjunto de principios y prácticas que buscan hacer el código más legible, mantenible y eficiente. Este apartado contiene conceptos fundamentales para escribir código limpio en C# y .NET.

## 📚 Temas Disponibles

### 1. Avoid Too Many Arguments In Functions
**Ubicación:** `concepts/04-clean-code/01-avoid-too-many-arguments/`

Una guía práctica sobre cómo reducir el número de argumentos en funciones usando objetos para encapsular datos relacionados.

### 2. Prefer IEnumerable<T> Over List<T> for Return Types
**Ubicación:** `concepts/04-clean-code/02-prefer-ienumerable-over-list/`

Guía sobre por qué preferir `IEnumerable<T>` sobre `List<T>` para tipos de retorno, mejorando flexibilidad, encapsulación y rendimiento.

### 3. Flattening Nested Collections Using SelectMany
**Ubicación:** `concepts/04-clean-code/03-nested-loops-vs-selectmany/`

Guía sobre cómo usar `SelectMany` para aplanar colecciones anidadas, reemplazando bucles anidados con código más legible y conciso.

### 4. Use MinBy or MaxBy Instead of OrderBy + First/Last
**Ubicación:** `concepts/04-clean-code/04-minby-maxby-instead-of-orderby/`

Guía sobre cómo usar `MinBy` y `MaxBy` para encontrar elementos extremos de manera más eficiente que `OrderBy` + `First`/`Last`.

### 5. Use the Proper Naming Convention
**Ubicación:** `concepts/04-clean-code/05-naming-conventions/`

Guía completa sobre las convenciones de nomenclatura en C#, incluyendo tabla de referencia para todos los tipos de objetos.

### 6. Applying C# Interpolated Strings for Cleaner Formatting
**Ubicación:** `concepts/04-clean-code/06-interpolated-strings/`

Guía sobre cómo usar interpolated strings en lugar de `string.Format` para código más legible y menos propenso a errores.

---

## 🎯 Objetivo

Este apartado está diseñado para ayudarte a:
- Escribir código más legible y mantenible
- Aplicar principios de Clean Code en C#
- Mejorar la calidad de tu código
- Facilitar el testing y mantenimiento

## 📖 Principios Fundamentales

- **Legibilidad**: El código debe ser fácil de leer y entender
- **Mantenibilidad**: El código debe ser fácil de modificar y extender
- **Testabilidad**: El código debe ser fácil de probar
- **Simplicidad**: La solución más simple suele ser la mejor

